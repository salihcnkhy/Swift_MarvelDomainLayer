import XCTest
@testable import MarvelDomainLayer

final class MarvelDomainLayerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MarvelDomainLayer().text, "Hello, World!")
    }
}
